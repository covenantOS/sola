{
  "name": "sola-plus",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "db:generate": "pnpm --filter @sola/database generate",
    "db:push": "pnpm --filter @sola/database push",
    "db:studio": "pnpm --filter @sola/database studio",
    "db:migrate": "pnpm --filter @sola/database migrate"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
